language: rust
rust:
  - nightly
cache: cargo

addons:
  apt:
    sources:
      - debian-sid
    packages:
      - binutils-arm-none-eabi

before_script:
  - export PATH="$PATH:$HOME/.cargo/bin"
  - cargo install --force rustfmt-nightly
  - which xargo || cargo install xargo
  - rustup component list | grep 'rust-src.*installed' || rustup component add rust-src
  - set -e

script:
  - cargo clean
  - xargo build --target thumbv6m-none-eabi
  - xargo build --target thumbv6m-none-eabi --examples --all-features
  - cargo fmt -- --write-mode=diff

after_script:
  - set +e
